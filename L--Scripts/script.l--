// we only have ints, strings and int arrays

int x = 5;
x + 10;
x - 1;
x * 2;
x / 3;

string w = "i work too!!!";

array a = {1,2,5,7};

// printing an array is that easy! (take that C#!)
print a;

// If statements

if(x >= 3)
{
    string k = "the if is true";
    print k;
}

// arrays index can only be assigned a new value;
a[0] = 5;
a[2] = x;

// can acces indexes;

print a[0];
print a[1];
print a[2];
print a[3];

// printing

print x;
print w;






